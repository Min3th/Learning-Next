import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['imageio.forbes.com','lisashea.com',"previews.123rf.com"],
  },
  // other config options here
};

export default nextConfig;